# Assets

The project logo lives here as `invarium-logo.svg` (referenced by the root `README.md`).

It's a scalable SVG, so it stays crisp at any size. To swap it, replace the file in place
or update the `<img src>` in the root README.
